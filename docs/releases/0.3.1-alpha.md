# SPINgen 0.3.1-alpha

- Release tag: pending
- Release commit SHA: the `0.3.1-alpha` release checkpoint commit
- Build configuration: `Release`
- Target runtime: `win-x64`
- Installer filename: `SPINgen_0.3.1-alpha_x64.msi`
- Portable filename: `SPINgen_0.3.1-alpha_win-x64.zip`
- Publish directory: `artifacts/publish/win-x64/`
- Supported OS: Windows 10/11 x64
- Release date: August 9, 2026

## Feature Summary

This checkpoint preserves the current `0.3.0-alpha` application feature set and adds release-facing project documentation improvements:

- initial SPINgen documentation suite
- user, operator, and developer guide coverage
- documentation index and navigation from the root README
- explicit documentation-is-code project policy
- refreshed release metadata and packaging identity

## Automated Build Intent

The intended release verification path is:

- `dotnet clean CEI_Report_Gen.sln --configuration Release`
- `dotnet restore CEI_Report_Gen.sln --runtime win-x64`
- `dotnet build CEI_Report_Gen.sln --configuration Release --no-restore`
- smoke/regression suite
- self-contained publish
- MSI build from verified publish output

## Manual Test Summary

- interactive GUI, installer, upgrade, and uninstall validation still require a normal Windows desktop session
- this release remains unsigned and may trigger SmartScreen
- release notes can be expanded with operator acceptance results after desktop validation

## Rollback Procedure

Source rollback point after tagging:

```powershell
git checkout <0.3.1-alpha tag>
```
